# テスト用のモード: 手元の .env.local に接続設定があっても、クラウド機能を無効にして起動する。
# 「クラウド未設定のとき」の動作を、いつでも同じ条件で確認するために使う。
# モード別のファイルは .env.local より優先されるので、接続先の実値はビルド結果にも入らない。
VITE_CLOUD_DISABLED=1
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_SUPABASE_PUBLISHABLE_KEY=
